// APP公共配置

// 应用名
export const APP_NAME = 'Web Aircraft Carrier';

// 是否启用PWA（渐进式Web App）
export const USE_PWA = false;

// 是否启用SPA（单页应用）
export const USE_SPA = true;

// 是否启用Socket.IO
export const USE_SOCKET = false;

// 开发模式下前端端口
export const DEV_FRONTEND_PORT = 8000;

// 开发模式下后端端口
export const DEV_SERVER_PORT = 3000;

// 文件上传大小(MB)
export const MAX_UPLOAD_FILE_SIZE = 0;

// Cookie
export const COOKIE_NAME = 'token';

// Chii Debug Port (web控制台)
export const CHII_PORT = 8080;
